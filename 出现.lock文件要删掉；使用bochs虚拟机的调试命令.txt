b 0x7c00	;在主引导扇区开始处设置断点
c		;持续执行至断点处
u/50		;反汇编50条指令
开始找需要着重关注的指令的物理地址，例如7c47
b 0x7c47
c
n		;到断点之后用n命令来单步执行，s也行
在ptr指向下一条指令 push 压栈时，先看一下段寄存器ss的值和栈指针寄存器sp的值
sreg		;看到段寄存器
r		;看到栈指针寄存器
n
。。。
r		;看变化，发现压栈后，已经变了-2，就是fffe
print-stack	;看栈的内容，只能每次显示10个数据，第一行是栈顶
